<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.carousel.MaskableFrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/my_card_maskable"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    app:shapeAppearanceOverlay="@style/RoundedAppearanceOverlay">

    <!-- 内部容器：圆角由 MaskableFrameLayout 处理 -->
    <LinearLayout
        android:id="@+id/my_card_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="24dp"
        android:paddingEnd="24dp">

        <ImageView
            android:id="@+id/my_card_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            app:tint="#FFFFFF" />

        <TextView
            android:id="@+id/my_card_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_weight="1"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="❯"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:alpha="0.6" />

    </LinearLayout>
</com.google.android.material.carousel.MaskableFrameLayout>
